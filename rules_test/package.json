{
  "name": "firestore-rules-test",
  "version": "1.0.0",
  "description": "Firestore security rules tests for Servisio Core",
  "scripts": {
    "test": "node --experimental-vm-modules test.js"
  },
  "dependencies": {
    "@firebase/rules-unit-testing": "^3.0.0",
    "firebase": "^10.0.0"
  }
}
